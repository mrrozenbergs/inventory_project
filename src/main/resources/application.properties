spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect
#spring.jpa.hibernate.ddl-auto= create-drop
spring.datasource.url=jdbc:mysql://localhost:3306/inventory_directory?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Ronalds1987

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

myapp.queries.users-query=select email, password from client where email=?
myapp.queries.roles-query=select c.email, r.role from client c inner join client_role ur on(c.client_id=cr.client_id) inner join role r on(cr.role_id=r.role_id) where c.email=?
# Admin username and password
myapp.admin.username=admin
myapp.admin.password=admin

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

spring.datasource.data=classpath:/sql/import-h2.sql

spring.datasource.tomcat.test-while-idle=true
spring.datasource.tomcat.validation-query=SELECT 1